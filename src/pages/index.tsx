import Layout from "@/components/Layout";
import Hero from "@/components/sections/Hero";

const index = () => {
  return (
    <Layout>
      <Hero />
    </Layout>
  );
};

export default index;
